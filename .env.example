# Copy to .env and fill in your local paths
# All paths are optional — features gracefully degrade when unset

# Path to BNG2.pl (for parity testing against reference implementation)
# Examples:
#   /usr/local/share/BioNetGen/BNG2.pl
#   C:\BioNetGen\BNG2.pl
#   /home/user/bionetgen/BNG2.pl
BNG2_PATH=

# Perl executable (defaults to "perl")
PERL_PATH=perl

# Emscripten SDK root (only needed for WASM builds)
EMSDK_PATH=
